storage:
  dbPath: /data/db
  journal:
    enabled: true

systemLog:
  destination: file
  logAppend: true
  path: /var/log/mongodb/mongod.log

net:
  bindIp: 127.0.0.1
  port: {{ mongodb_port }}

replication:
  replSetName: "{{ mongodb_replica_name }}"